# ================================================================
# ZeRag 生产环境配置示例
# 复制此文件为 .env 并填写真实值
# ================================================================

# ── 数据库（必填，指向外部 PostgreSQL + pgvector）──────────────
DATABASE_URL=postgresql://postgres:your_password@your_db_host:5432/zerag

# ── 安全 ──────────────────────────────────────────────────────
# 用于签发 JWT，生产环境请使用随机强密码
# 生成命令：openssl rand -hex 32
SECRET_KEY=change-this-to-a-strong-random-secret

# 数据源密码加密密钥（Fernet 格式）
# 生成命令：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-key-here

# ── 通义千问 API ───────────────────────────────────────────────
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# ── 前端暴露端口 ────────────────────────────────────────────────
FRONTEND_PORT=80

# ── CORS（前端访问的域名，多个用逗号分隔）─────────────────────
# 示例：https://zerag.example.com,http://localhost
CORS_ORIGINS=http://localhost

# ── 嵌入 & Reranker 模型（可选，默认值已内置）──────────────────
# EMBEDDING_MODEL=BAAI/bge-m3
# EMBEDDING_DIMENSION=1024
# RERANKER_MODEL=BAAI/bge-reranker-base
# ENABLE_RERANKER=true

# ── 其他应用配置（可选）────────────────────────────────────────
# 是否开放用户自助注册（生产建议关闭）
ALLOW_REGISTER=false
